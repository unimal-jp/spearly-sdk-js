name: add version tag
on:
  push:
    branches:
      - next-version

jobs:
  tag:
    name: add tag
    runs-on: ubuntu-latest
    if: ${{ !contains(github.event.head_commit.message, '[SKIP]') }}
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main
          fetch-depth: 0
      - env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "github-actions+${GITHUB_ACTOR}@users.noreply.github.com"
          git switch next-version
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ github.event_name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-${{ github.event_name }}-
      - run: npm i
      - run: ./scripts/next-version.js
      - env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=`node -pe "require('./package.json').version"`
          git add .
          git commit -m "update version: ${VERSION}"
          git push https://${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git next-version
          git tag v$VERSION
          git push https://${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git v$VERSION
